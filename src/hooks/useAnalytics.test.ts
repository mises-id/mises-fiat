import { useAnalytics } from "./useAnalytics";
// import { initializeApp } from 'firebase/app';
// import { getAnalytics } from 'firebase/analytics';

describe('useAnalytics', ()=>{
  it('test useAnalytics', () => {
    useAnalytics()
    // expect(getAnalytics).toBeCalledTimes(1);
  })
})